{
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "tsc": "tsc -b",
        "deploy": "bash build/deploy.sh",
        "lint": "tslint */source/*.ts",
        "lint:fix": "tslint */source/*.ts --fix"
    },
    "devDependencies": {
        "@types/node": "^10.12.18",
        "semver": "^5.6.0",
        "ts-loader": "^5.3.2",
        "typescript": "^3.2.2",
        "tslint": "^5.12.1"
    }
}